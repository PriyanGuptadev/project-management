<div class="p-4 bg-gray-100 rounded-lg mb-2">
  <p class="text-gray-800 font-medium"><%= comment.content %></p>
  <p class="text-sm text-gray-500"><%= comment.user.email %> at <%= comment.created_at.strftime('%b %d, %Y %H:%M %p') %></p>
</div>

